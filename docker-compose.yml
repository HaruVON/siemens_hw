version: "3.9"

networks:
    siemens_hw:

volumes:
    siemens_hw_logs: {}

services:
    web:
        build: .
        restart: always
        ports:
            - "80:80"
        volumes:
            - siemens_hw_logs:/var/www/siemens_hw/api/logs:rw
